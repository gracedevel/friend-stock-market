{
  "name": "pretend-stock-market-free",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "serve": "firebase emulators:start",
    "deploy": "firebase deploy",
    "deploy:hosting": "firebase deploy --only hosting",
    "deploy:firestore": "firebase deploy --only firestore:rules,firestore:indexes"
  },
  "dependencies": {
    "firebase": "^12.0.0"
  }
}
